# Product scope

## Goal

Build an open-source, agent-first SEO product that delivers OpenSEO's SEO capabilities through a portable core, local-first commands and portable agent skills, with project-owned data and no mandatory OpenSEO server. Full SEO capability parity is the destination; phases in the plan are delivery order, not a reduced product goal. The product must be useful to agents beyond Pi and maintainable by contributors outside its founding team.

A user asks their coding agent to investigate or improve a website. Skills guide the investigation; commands supply evidence and operations; the agent can use its existing file tools to implement changes. Every capability must be usable without a browser UI. Programmatic output must remain usable without interpreting a report or scraping terminal text.

## Users and boundaries

- Primary users: developers, SEO practitioners and teams using agents to research and improve websites. Pi is the first supported development path; any agent able to run commands should be able to use the product. Local-first does not mean single-user-only.
- The CLI runs on demand. It may call external APIs. Scheduled or remote work can invoke the same operations without introducing a required web service.
- Project context, saved results, and reports live in a project-owned directory with portable, documented formats. API credentials live outside the project or in environment variables, never in project files. Projects can be shared using ordinary version control for selected files; concurrent editing of one local history database is not promised.
- No hosted-account signup, workspace membership, team permissions, subscriptions, credit resale, referrals, or required Web UI. Provider usage fees and quotas still apply. Optional integrations must not turn into a mandatory hosted account.
- OpenSEO's in-app SAM chat interface is not reproduced. Its underlying SEO workflows must be available to the user's existing agent through local commands and skills; chat UI/session parity is outside scope.

## Capability inventory

Baseline: OpenSEO commit `0ffff93101043aad7600a3b6a499a0cd2887ef49`. Paths below are relative to that commit. This inventory was checked against its registered MCP tools **and** SEO-related application server functions/services; entries group related operations rather than treating every endpoint as a separate product capability. The linked upstream sources define the required behavior of each row: a row is `verified` only when the local command covers the operations, inputs and result distinctions of those sources. Paths are representative, not exhaustive; `scripts/upstream-coverage.mjs` lists upstream modules this file never names so a review can judge them instead of relying on memory. See [PLAN.md](PLAN.md) for delivery phases. `not started` means **no local command, fixture or parity evidence**; `in progress` means a narrower local slice exists; `verified` requires a comparable local command, fixture/live evidence and a read-back for mutations; `blocked` requires a documented external dependency. No row currently has full parity.

| Capability | OpenSEO baseline source | Status | Local evidence / difference |
| --- | --- | --- | --- |
| Project site and market initialization | `src/serverFunctions/projects.ts`, `src/server/mcp/tools/create-project.ts` | in progress | `agenticseo init` canonicalizes the domain with OpenSEO's parser and resolves the market with its `resolveMarketInput` rules (supported country, language defaulting to the country's, pair checked), accepting a two-letter country code in place of the picker; `test/project.test.ts`. Settings are edited in `.agenticseo/project.json` and validated on every read; no list/archive, since a project is one directory. |
| Project positioning, competitors, key pages and preferences | `src/serverFunctions/projectContext.ts`, `src/server/features/project-context/services/ProjectContextService.ts` | in progress | `agenticseo context` validates `.agenticseo/context.json` with upstream's names, caps, roles and domain/URL normalization; `test/project.test.ts`. Agents edit the file instead of sending patch ops, so upsert by normalized domain/URL becomes a duplicate check. No per-entry author or update time. |
| Research log and project history | `src/server/features/project-context`, `src/server/features/projects` | in progress | `researchLog` in `.agenticseo/context.json`: agent-written `<what>: <inputs>. Verdict: <conclusion>` entries, shown newest 20 within 90 days; skills apply upstream's 30-day reuse rule. The agent writes the date, where upstream's server stamps it; `context` supplies `today` and rejects later dates, but cannot detect an earlier wrong date. Old entries are hidden, not pruned. |
| Keyword overview (volume, CPC, difficulty, intent) | `src/server/lib/dataforseo/labs.ts`, `src/server/lib/dataforseo/keyword-metrics.ts` | in progress | `agenticseo keywords TERM... [--clickstream]` runs OpenSEO's ported `fetchKeywordMetricsForList` (Labs or Google Ads by market, with competition, level and monthly searches) through its ported DataForSEO core; `test/keywords.test.ts` and authorized live calls on 2026-09-23 for US/en (Labs) and Andorra/ca (Google Ads). Returned metrics become the project's latest snapshot for saved keywords (upstream has the agent copy them into `save_keywords`). Missing monthly volumes stay null where upstream writes 0, and a term whose row has no metric at all is reported in `missingKeywords` rather than as a row. |
| Keyword discovery, suggestions and research scopes | `src/serverFunctions/keywords.ts`, `src/server/features/keywords/services/research/research.ts`, `src/serverFunctions/domain.ts` | in progress | `agenticseo research "SEED"` runs the ported research service (related → suggestions → ideas, Google Ads ideas outside Labs, 24-hour cache in `.agenticseo/cache/`); `test/research.test.ts` and a live US/en call. One seed per call where the MCP tool batches 1–5; no manual source mode; researched metrics are not yet stored for saved keywords. Domain research scopes are not started. |
| SERP results and per-keyword analysis | `src/server/mcp/tools/get-serp-results.ts`, `src/serverFunctions/keywords.ts` | in progress | `agenticseo serp "QUERY" [--depth]` ports `get_serp_results` (all result types, no cache, empty SERP as an empty result); `test/research.test.ts` and a live US/en call. One query per call where the MCP tool batches 1–10. The app's cached organic SERP analysis with backlink columns is not ported. |
| Saved keywords, refresh, tags and removal | `src/serverFunctions/keywords.ts`, `src/server/features/keywords/repositories/SavedKeywordTagsRepository.ts` | in progress | `agenticseo saved add/list/tag/rename-tag/delete-tag/remove/refresh` port OpenSEO's saved-keyword service, tag rules and list filters onto the project SQLite database (`src/store.ts`), and `research` stores metrics as upstream's `persistRows` does; `test/saved.test.ts`, including concurrent writers, and a live refresh. Tags are addressed by name instead of id; saving takes no caller-supplied metrics because `research` and `keywords` store what they fetch; tag colors are stored but not rendered. `agenticseo query` adds read-only SQL for agents. |
| Saved keyword export | `src/serverFunctions/keywords.ts` (`exportSavedKeywords`) | in progress | `agenticseo saved export` writes OpenSEO's CSV columns with its quoting, rounding and formula protection, or JSONL rows; `test/saved.test.ts`. Google Sheets export is not ported. |
| Domain overview and keyword suggestions | `src/serverFunctions/domain.ts`, `src/server/features/domain/services/DomainService.ts` | in progress | `agenticseo domain TARGET` ports `getOverview` (12-hour cache, scope labels); keyword suggestions are `agenticseo ranked TARGET --sort traffic_estimate --limit 100`, the same request without its 12-hour cache. `test/domain.test.ts` and live US calls. Upstream's overview leaves backlinks and referring domains null, as does this port. |
| Ranked keywords, pages, market/scope filters and pagination | `src/serverFunctions/domain.ts`, `src/server/mcp/tools/dataforseo-research-tools.ts` | in progress | `agenticseo ranked` ports `get_ranked_keywords` with OpenSEO's scope filters, volume/rank/brand filters, result types, sort and offset paging; `test/domain.test.ts` and a live subfolder call. Labs-only markets exit 2 where upstream falls back to the US. `agenticseo pages` ports the app's pages view (`relevant_pages` with scope, URL-term and range filters, sort, page paging, 12-hour cache) without its free-text search, which `--include` covers. The app's keyword-page view with KD/CPC/rank range filters is not ported; `ranked` is the agent path. |
| SERP competitors and comparison | `src/server/mcp/tools/dataforseo-research-tools.ts` (`findSerpCompetitorsTool`) | in progress | `agenticseo competitors KEYWORD...` ports `find_serp_competitors` (default organic and local pack, domain exclusion with subdomains, sort, paging); `test/domain.test.ts` and a live US call. |
| Backlink overview and profile | `src/serverFunctions/backlinks.ts`, `src/server/features/backlinks/services/BacklinksService.ts` | in progress | `agenticseo backlinks overview` and `backlinks links` port `get_backlinks_overview` and `get_backlinks_profile` over OpenSEO's `BacklinksService` (summary, monthly trends, top referring domains, subfolder totals, spam cutoff, billing-issue classification, six-hour cache); `test/backlinks.test.ts` and live calls on 2026-09-23. |
| Referring domains, backlinks, top pages, subfolders and filters | `src/serverFunctions/backlinks.ts`, `src/server/features/backlinks/services/backlinksApiFilters.ts` | in progress | `agenticseo backlinks domains/pages/links` port the app's three tabs with `backlinksApiFilters` (include/exclude terms, ranges, link type, lost/broken, source domain), sort fields, page sizes and the subfolder scope filter; `test/backlinks.test.ts`. |
| Backlink history/snapshot | `src/server/features/dashboard/repositories/BacklinkSnapshotRepository.ts` | in progress | Twelve months of history (backlinks, referring domains, rank, new/lost) come with `backlinks overview`. The dashboard's daily project snapshot is a dashboard summary and is reviewed with the dashboard row in Phase 6. |
| Public Ahrefs domain rating enrichment | `src/serverFunctions/ahrefs.ts` | in progress | `agenticseo domain-rating` ports the lookup with its normalization, 0-as-no-rating rule and one-day cache; failures are reported instead of returned as null. Ahrefs now requires a free APIv3 key on this endpoint (the pinned upstream sends none and silently shows no ratings), so `AHREFS_API_KEY` is sent as a Bearer token. Verified live only for missing and rejected keys; a successful live lookup needs a real key. |
| SERP location and market resolution (name to `locationName`/location code) | `src/server/mcp/tools/search-serp-locations.ts`, `src/serverFunctions/serp-locations.ts`, `src/server/lib/dataforseo/serp-locations.ts` | in progress | Market resolution is done: per-call `--location`/`--language` overrides follow OpenSEO's `resolveMarket` and `assertLanguageForLocation` (`test/project.test.ts`). `agenticseo rank locations "PLACE" [--location COUNTRY]` ports `search_serp_locations`: the country's free registry, cached 30 days in the project cache, ranked by OpenSEO's matcher (`test/rank.test.ts`; live on 2026-09-24). |
| Local business search, profiles and categories | `src/server/mcp/tools/dataforseo-research-tools.ts`, `src/server/mcp/tools/local-seo-tools.ts` | in progress | `agenticseo local businesses/profile/categories` port `search_local_businesses`, `get_business_profile` and `list_business_categories` (coordinate formats, rating/review/claim filters, cid/place_id prefixes, check_url merge, seven-day category cache); `test/local.test.ts` and live calls on 2026-09-23. Malformed Q&A or category rows fail instead of being dropped. |
| Maps / Local Finder SERPs and local rank grid | `src/server/mcp/tools/dataforseo-research-tools.ts`, `src/server/mcp/tools/local-seo-tools.ts` | in progress | `agenticseo local serp` and `local grid` port `get_local_serp_results` and `get_local_rank_grid` (viewport zoom derived from spacing and latitude, north-first grid, target matching, batches of three, stop on credentials, systemic failure surfaced); `test/local.test.ts` and a live 3×3 grid. |
| Business reviews, posts/updates and Q&A | `src/server/mcp/tools/local-seo-tools.ts`, `src/server/lib/dataforseo/business.ts` | in progress | `agenticseo local reviews/posts/questions` port `get_business_reviews`, `get_business_updates` and `get_google_business_questions`: one billed task post that is never retried, high priority, six free polls four seconds apart, a resumable task id (`google:`/`extended:` for reviews) and a collection failure that keeps it; `test/local.test.ts` and live calls on 2026-09-23, including a post collected by resume at no cost. |
| AI brand lookup, mentions and share of voice | `src/serverFunctions/ai-search.ts`, `src/server/features/ai-search/services/brandLookup.ts`, `src/server/features/ai-search/services/shareOfVoice.ts` | in progress | `agenticseo ai brand BRAND_OR_DOMAIN [--competitors ...]` ports Brand Lookup. It calls DataForSEO's LLM Mentions endpoints for ChatGPT (US/en only, as the provider serves it) and Google AI Overview, and returns mentions, AI search volume, monthly trend, cited pages, example prompts, and share of voice for up to five competitors. A complete result is cached for a day. Local changes: failed sub-calls are listed in `failedCalls` instead of logged, and a rejected key stops the lookup instead of returning empty platforms. `test/ai.test.ts`. Verified live on 2026-09-24: one lookup with two competitors made eight calls and cost $0.839. |
| AI cited sources and prompt exploration | `src/server/features/ai-search/services/citedSources.ts`, `src/server/features/ai-search/services/promptExplorer.ts` | in progress | Cited sources come with `ai brand`, as upstream derives them (`deriveCitedSources`). `agenticseo ai prompt "PROMPT" [--models ...] [--brand NAME]` ports Prompt Explorer: one prompt to ChatGPT, Claude, Gemini and Perplexity through DataForSEO's LLM Responses, with citations (unsafe links dropped), fan-out queries and brand highlighting. Each answer is cached for a week, and a failed model shows the provider's reason; upstream shows a generic message. Stdout shows the first 1,500 characters of each answer, and the evidence keeps them in full. Verified live on 2026-09-24: four models cost $0.153. |
| Robots/sitemap-aware discovery and safe crawl | `src/server/lib/audit/discovery.ts`, `src/server/lib/audit/url-policy.ts`, `src/server/workflows/SiteAuditWorkflow.ts` | in progress | `agenticseo audit start` runs OpenSEO's discovery, URL policy, page fetching, crawl window and 429 throttle unchanged. One local worker leases URLs from a SQLite frontier in batches of 25, replacing the Cloudflare Workflow and Durable Object. The start-URL DNS check uses `node:dns` and fails on an unresolvable host; `--allow-private` admits local targets. `test/audit.test.ts` covers robots.txt, a 429 retry, a killed worker resumed without re-fetching saved pages, and takeover. A live `audit start https://badseo.dev --wait` on 2026-09-24 crawled 42 pages in 42 s. |
| Page issues and cross-page checks | `src/server/lib/audit/issues/page-reporters.ts`, `src/server/lib/audit/issues/multipage-checks.ts` | in progress | Page reporters and the duplicate and redirect checks are ported unchanged. The broken-link and orphan queries run on the project database instead of the scratchpad Durable Object. The live badseo.dev audit matched the declared issues on 40 of 42 fixture pages. The other two are 429 fixtures the deployed site does not serve yet, and the 429 path is tested locally instead. |
| Audit runs, progress, partial failure, history and deletion | `src/serverFunctions/audit.ts`, `src/server/features/audit/services/AuditService.ts` | in progress | `audit start`, `status`, `resume`, `list` and `delete` are built. Audits run in a background worker with a heartbeat and resume from the recorded phase. Failures are stored with their message and can be resumed. Deleting a running audit stops its worker at the next write (`test/audit.test.ts`). |
| Audit issue/page retrieval and raw evidence | `src/server/mcp/tools/site-audit-tools.ts`, `src/serverFunctions/audit.ts` | in progress | `audit issues` and `audit pages` port `get_audit_issues` and `get_audit_pages`: the same filters and defaults, severity-first ordering and a per-type summary. Each type's `howToFix` appears once in the summary instead of on every row. An unknown issue type is an error. `audit export` writes the results view's issues or pages CSV columns, or JSON lines. Every stored page column is readable with `query`. |
| Optional Lighthouse issues and export | `src/serverFunctions/lighthouse.ts`, `src/server/lib/audit/lighthouse.ts` | in progress | `audit start --lighthouse` ports the Lighthouse phase: the sample of the start page plus one page per URL template (at most ten), mobile and desktop, through DataForSEO's billed, never-retried Lighthouse call. OpenSEO's compact stored payload is kept in the project database instead of R2. `audit lighthouse` lists scores and Core Web Vitals; `--result` gives one check's issues (OpenSEO's `getAuditLighthouseIssues`, largest savings first). `audit export --table performance` writes the performance CSV. Stored results are the checkpoint, so a resumed audit does not pay twice. A rejected key stops the audit instead of recording a failure on every page. Verified live on 2026-09-24: 12 checks on a real site for $0.06. OpenSEO's download of the full or per-category issue file is not ported; `query` reads `payload_json`. |
| Rank tracker config, targets and keyword management | `src/serverFunctions/rank-tracking.ts`, `src/server/features/rank-tracking/services/RankTrackingService.ts` | in progress | `agenticseo rank create/update/list/show/archive/add/remove` port the tracker and keyword operations with `create_rank_tracker`'s defaults (mobile, depth 40, manual). A domain plus market, or plus city, is one tracker; re-creating an archived one reactivates it with its keywords. Keywords are lowercased unless `--match-case`. A city name is checked against DataForSEO's free sandbox; if the sandbox cannot answer, the tracker is saved with a warning. Keywords over the 1,000 limit and unknown keyword ids are reported; upstream skips them silently. `test/rank.test.ts`. |
| Rank cost estimation, manual runs and queued provider polling | `src/serverFunctions/rank-tracking.ts`, `src/server/workflows/RankCheckWorkflow.ts` | in progress | `agenticseo rank estimate` gives the nominal cost from OpenSEO's price table. `agenticseo rank run ID [--keywords IDS]` ports the manual live check: ten keywords per batch on every device, stopping the crawl once the domain is found, with snapshots saved per batch and upstream's partial-result rules. A run records its DataForSEO cost, including charged failures. A rejected key stops the run after its batch. A run whose process died is closed by the next command: it is completed with a note when it saved results, instead of being failed with them hidden. In a live check on 2026-09-24, a four-page check cost $0.008 when the domain was not found; OpenSEO's price table gives $0.0065. Scheduled checks use the queued path: every pair is posted in one request of up to 100 tasks, each accepted task id is stored at once, and the process polls for about 15 minutes (4/2/2/2/2/3 minutes). Rejected, failed and unfinished tasks get one live check. On 2026-09-24 a four-page queued task cost $0.0024; the price table gives $0.00195. |
| Scheduled checks and duplicate-run prevention | `src/server/features/rank-tracking/services/scheduledRankChecks.ts`, `src/server/features/rank-tracking/services/rankCheckRunGuards.ts` | in progress | `agenticseo rank due` is the entry point for the host scheduler, which calls it hourly; `rank schedule` prints the crontab line and does not install it. Each call does two things. First, it adopts scheduled runs whose process died and collects their stored, paid tasks without posting again. Second, it starts a queued check for every due tracker: next-check time is the compare-and-set token, a missed slot runs once and the next slot stays anchored, and a tracker with no keywords is skipped with a recorded reason. The per-call budget is 1,000 task units. One running check per tracker is enforced by a partial unique index, and a run's heartbeat and process id show whether it is alive. A rejected key fails the call with exit 3. `test/rank.test.ts` covers the queue, fallback, adoption, a race and a rejected key. Verified live on 2026-09-24: a `rank due` killed after posting was adopted by the next call, which collected both tasks for no extra cost. |
| Ranking history, trends and position matrix | `src/serverFunctions/rank-tracking.ts`, `src/server/features/rank-tracking/repositories/RankTrackingRepository.ts` | in progress | `rank show ID [--compare 1d|7d|30d|90d]` ports `get_rank_tracker` and the latest results: each keyword's position, ranking URL and SERP features, and its position at the start of the period or its earliest check. `rank history`, `rank trend` and `rank matrix` port the keyword history, the top-3/4-10/11-20/not-ranking trend and the by-date matrix. The matrix is pivoted per keyword and marks runs that did not check a keyword. `rank metrics` refreshes volume, difficulty and CPC, city-scoped for local trackers. `test/rank.test.ts`. |
| Search Console connection, property selection and revocation | `src/serverFunctions/gsc.ts`, `src/serverFunctions/googleAccounts.ts` | in progress | `agenticseo google connect/accounts/disconnect` replaces OpenSEO's Better Auth grant. It uses Google's installed-app flow (PKCE and a loopback redirect) with the user's own Desktop OAuth client, and stores refresh tokens per Google account in an owner-only file in the user's config directory, not the project. A consent that leaves out a product is reported. Disconnecting revokes the grant at Google. `gsc sites/use/disconnect` port property listing and selection: unverified properties are refused, and when no account is named the property is found across accounts. The mapping is `searchConsole` in `project.json`. A revoked or expired grant exits 3 with a reconnect hint instead of reporting `connected: false`. `test/google.test.ts`, with Google mocked, and verified live on 2026-09-24 on kua.ai (see PLAN.md, Phase 5). |
| Search performance, opportunities and table export | `src/serverFunctions/searchPerformance.ts`, `src/server/mcp/tools/search-console-tools.ts` | in progress | `gsc performance` ports `get_search_console_performance`: dimensions, date ranges with the 16-month and 3-day-lag clamps, filters wrapped in a filter group, local metric filters over the top 1,000 rows, and paging in Google's row space. `gsc report` ports the overview (totals, previous period, striking distance, countries) and `gsc export` the query or page table. Verified live on 2026-09-24 on kua.ai. |
| Search Console URL inspection | `src/server/mcp/tools/search-console-tools.ts` | in progress | `gsc inspect URL...` ports `inspect_urls` for up to ten URLs, with per-URL errors. A denied or expired grant stops the batch, as OpenSEO's spec intends; upstream records a 401/403 against each URL. Verified live on 2026-09-24 on kua.ai. |
| GA4 authorization, property selection and revocation | `src/serverFunctions/ga4.ts`, `src/serverFunctions/googleAccounts.ts` | in progress | The Google grant is shared with Search Console (`google connect --for analytics`). `ga4 properties/use/disconnect` port property discovery and selection: account summaries across connected accounts, with the property's time zone and currency recorded as `analytics` in `project.json`. `test/ga4.test.ts`, with Google mocked, and verified live on 2026-09-24 on kua.ai (see PLAN.md, Phase 5). |
| GA4 organic overview, landing pages, page performance and acquisition | `src/server/mcp/tools/google-analytics-tools.ts`, `src/server/features/ga4/services/Ga4ReportingService.ts` | in progress | `ga4 overview` and `ga4 report landing-pages|page-performance|traffic-acquisition` run OpenSEO's report definitions, normalization and enhancements unchanged: Organic Search by default, the property's time zone, end dates clamped to the last complete day, and restricted metrics as null. Refusals exit with a code: reconnect and lost property access exit 3, quota and a disabled Data API exit 4. Verified live on 2026-09-24 on kua.ai. |
| GA4 key events, ecommerce, site search and audience | `src/server/mcp/tools/google-analytics-tools.ts` | in progress | `ga4 report key-events|ecommerce|site-search|audience` with each tool's breakdowns, channel option, previous-period comparison where upstream allows it, and `--only-with-transactions`. Options a report does not take are refused. Verified live on 2026-09-24 on kua.ai. |
| GA4 measurement health and search opportunities | `src/server/features/ga4/services/Ga4MeasurementHealthService.ts`, `src/server/features/ga4/services/SearchOpportunityService.ts` | in progress | `ga4 health` ports the measurement check (web streams, enhanced measurement, key events, custom definitions). `ga4 opportunities` joins Search Console page demand with GA4 organic landing pages, as upstream does, and needs both connections. Verified live on 2026-09-24 on kua.ai. |
| Report save/list/read/delete and HTML output | `src/server/mcp/tools/report-tools.ts`, `src/server/features/reports/services/ReportService.ts` | in progress | `agenticseo reports` indexes `.agenticseo/reports/*.md` with an optional same-name `.html` export, enforcing upstream's title, summary and HTML caps and refusing duplicate titles; `test/reports.test.ts`. Agents write, replace and delete files directly. Differences: no skill/author/template columns, `updatedAt` is the file time, and listing has no paging. |
| Report templates and briefs | `src/server/mcp/tools/report-template-tools.ts`, `src/serverFunctions/reportTemplates.ts` | in progress | Templates are `.agenticseo/templates/*.md`, listed as `reportTemplates` (name, description) by `agenticseo context` as upstream's context digest does; `test/project.test.ts`. No brief generation or template id on reports. |
| Dashboard-style SEO summaries and backlink snapshots | `src/serverFunctions/dashboard.ts`, `src/server/features/dashboard/services/DashboardService.ts` | not started | — |
| Task skills: project setup, keyword research/clustering | `plugins/openseo/skills/seo-project-setup/SKILL.md`, `plugins/openseo/skills/keyword-research/SKILL.md`, `plugins/openseo/skills/keyword-clustering/SKILL.md` | in progress | `.agents/skills/seo-project-setup`, `keyword-research` and `keyword-clustering` are adapted from upstream in full: MCP tools become `agenticseo` commands, and `update_project_context` becomes edits to `.agenticseo/context.json` validated by `agenticseo context`. Search Console and Analytics are connected with `google connect` and `gsc use`/`ga4 use`. |
| Task skills: audits, competition, local SEO, links, reports and coaching | `plugins/openseo/skills/` (seo-audit, competitive-landscape, competitor-analysis, local-seo, link-prospecting, seo-report, seo-coach) | in progress | All seven are adapted in `.agents/skills/` with upstream's goals, workflows, output sections and guardrails. Local changes: reports are Markdown plus an HTML page from `seo-report/template.html`, checked by `agenticseo reports`; the closing link points at the skill file on GitHub; SERPs are one query per call; paid lookups name their cost and evidence file. `competitive-landscape` also offers `ai brand` for AI-answer share of voice. |

Scope boundary: hosted report share links (`src/serverFunctions/reports.ts` `shareReport`/`unshareReport`), workspace and organization permissions (`src/serverFunctions/workspace.ts`, `src/serverFunctions/organization.ts`), subscriptions and credits (`src/serverFunctions/billing.ts`), onboarding and activation widgets (`src/serverFunctions/onboarding.ts`, `src/server/features/activation`), SAM chat sessions and their access checks (`src/serverFunctions/sam.ts`, `src/serverFunctions/samAccess.ts`, `src/server/features/sam`), hosted account identity (`src/server/mcp/tools/whoami.ts`) and hosted app plumbing (`src/serverFunctions/config.ts`, `src/serverFunctions/middleware.ts`) are tied to the Web product; SAM's SEO workflows are delivered through the user's own agent instead of an in-app chat (see Users and boundaries), and account identity has no local equivalent because there is no hosted account; local HTML export can replace file sharing **but is not equivalent to a hosted public link**. Reassess this gap in the final parity review, not as a prerequisite for a usable local SEO tool. All tracked provider and Google operations remain unverified until exercised with authorized access; the live keyword-overview calls only validate that endpoint in US/en.

Data access is not the same as agent workflow parity. Preserve or adapt OpenSEO's public SEO skills where useful; discoverable task workflows, not a flat list of commands, are the primary agent experience. Commands supply data and operations; the agent owns interpretation, writing, and changes to the user's website. A normal run should return a concise finding with provenance and a local evidence reference, and let the agent retrieve specific rows or pages only when needed. Paid operations do not require product-level budget approval or website-edit approval; those choices belong to the user and their agent.

## Acceptance definition

For each inventory row, demonstrate a local invocation that produces the comparable OpenSEO result for a representative fixture or authorized live project, preserves important distinctions (target scope, market, dates, source and missing data), and leaves inspectable project-local evidence. For a mutation, verify that a second invocation can read the changed state. Test a task from the user's request through to a concise agent reply in Pi, without flooding its context with raw results; repeat it on at least one non-Pi shell-capable agent before a public release.

The final parity review must also inspect OpenSEO's application-only SEO endpoints, not just its MCP registration list. Document upstream-only behavior that cannot be reproduced because of provider access, licensing, or platform limitations rather than silently calling it complete. A public release also needs a documented install path, reproducible checks, a clear license and usable agent guidance for at least one non-Pi client.
